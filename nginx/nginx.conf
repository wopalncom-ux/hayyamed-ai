events {}
http {
  upstream web { server hayyamed-web:3000; }
  upstream api { server hayyamed-api:4000; }
  server {
    listen 80;
    location / { proxy_pass http://web; }
    location /api { proxy_pass http://api; }
  }
}
